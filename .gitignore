node_modules/
.vercel
.env*
*firebase-adminsdk*.json
.DS_Store
backups/
.vscode/
# Local, per-machine record of which scheduler failures have been emailed —
# state, not source. Deleting it only re-sends alerts for still-open failures.
.deliver-brochures-alerts.json

# Screenshot output from tests/*-preview.mjs — regenerated on demand.
tests/out/

# Preview screenshot folders. Most previews take an out-dir argument and
# default to tests/out/, but a few default to the repo root — ignored here
# rather than leaving them to be committed by accident.
attio-preview/
conv-out/
dash-out/
pipeline-preview/
property-mobile/
scroll-out/
tailortalk-preview/
